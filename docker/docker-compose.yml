services:
  catt-app:
    build:
      context: .. 
      dockerfile: docker/Dockerfile
    container_name: catt-dev
    network_mode: host
    volumes:
      - ./config_test:/config
    environment:
      - PYTHONUNBUFFERED=1