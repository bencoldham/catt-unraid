[project]
name = "unraid-catt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "catt>=0.13.1",
    "loguru>=0.7.3",
]

[tool.uv.workspace]
members = [
    "example",
]
